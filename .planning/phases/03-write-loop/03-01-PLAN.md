---
phase: 03-write-loop
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/tag-normalize.test.ts
  - tests/actions.test.ts
  - package.json
autonomous: false
requirements:
  - TAG-02
  - ENTR-01
  - ENTR-02
  - ENTR-03

must_haves:
  truths:
    - "npx vitest run exits 0 with all tests green"
    - "Tag normalization logic (trim, lowercase, deduplicate, filter-empty) is covered by failing-then-passing unit tests"
    - "createEntry insert-returning pattern is covered by a unit test against in-memory SQLite"
    - "updateEntry and deleteEntry (with cascade) are covered by unit tests"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with node environment and tests/** glob"
    - path: "tests/tag-normalize.test.ts"
      provides: "Unit tests for normalizeTag (trim, lowercase) and server-side normalization pipeline"
    - path: "tests/actions.test.ts"
      provides: "Unit tests for createEntry, updateEntry, deleteEntry Drizzle patterns against in-memory DB"
  key_links:
    - from: "tests/actions.test.ts"
      to: "src/lib/db/schema.ts"
      via: "import * as schema — table definitions must match"
      pattern: "import.*schema"
    - from: "vitest.config.ts"
      to: "tests/**/*.test.ts"
      via: "include glob"
      pattern: "tests/\\*\\*"
---

<objective>
Bootstrap test infrastructure and write the validation layer that Waves 1–3 depend on.

Purpose: Nyquist compliance — automated feedback after every subsequent task. The tag-normalize tests prove TAG-02 normalization logic before it is wired into any component. The actions tests prove the Drizzle insert/update/delete patterns against an in-memory SQLite instance so Wave 1 server-action code is validated immediately after being written.

Output: vitest.config.ts, tests/tag-normalize.test.ts, tests/actions.test.ts — all green on first `npx vitest run`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-write-loop/03-RESEARCH.md
@.planning/phases/03-write-loop/03-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Install @uiw/react-md-editor and Vitest, bootstrap shadcn AlertDialog</name>
  <files>package.json, vitest.config.ts</files>
  <read_first>
    - /Users/zacharylim/Documents/Claude/Projects/dev-journal/package.json — check existing deps before installing
    - /Users/zacharylim/Documents/Claude/Projects/dev-journal/.planning/phases/03-write-loop/03-RESEARCH.md §New — Phase 3 installs table and §Installation section
  </read_first>
  <action>
    Run the following commands in the project root (cd /Users/zacharylim/Documents/Claude/Projects/dev-journal):

    1. Install @uiw/react-md-editor if not already in package.json:
       npm install @uiw/react-md-editor

    2. Install Vitest as a dev dependency:
       npm install -D vitest

    3. Bootstrap shadcn AlertDialog (answer Y when prompted to create components.json):
       npx shadcn@latest add alert-dialog

       This will install: @radix-ui/react-alert-dialog, class-variance-authority, clsx, tailwind-merge, lucide-react
       It will create: src/components/ui/alert-dialog.tsx and src/lib/utils.ts

    After all installs complete, create vitest.config.ts in the project root with this configuration:
    - Import defineConfig from 'vitest/config'
    - Set test.environment to 'node'
    - Set test.globals to true
    - Set test.include to ['tests/**/*.test.ts']

    Do NOT add a Next.js plugin to vitest.config.ts — tests are pure Node.js logic with no Next.js-specific imports.
  </action>
  <what-built>@uiw/react-md-editor, Vitest, and shadcn AlertDialog dependencies installed; vitest.config.ts created</what-built>
  <how-to-verify>
    1. Run: cat package.json | grep -E '"@uiw/react-md-editor|vitest|@radix-ui/react-alert-dialog'
       — expect all three present
    2. Run: ls src/components/ui/alert-dialog.tsx src/lib/utils.ts
       — expect both files to exist
    3. Run: cat vitest.config.ts
       — expect defineConfig with environment: 'node', globals: true, include: ['tests/**/*.test.ts']
    4. Confirm no install errors in terminal output
  </how-to-verify>
  <resume-signal>Type "approved" once all four checks pass, or describe any failures</resume-signal>
  <acceptance_criteria>
    - package.json contains "@uiw/react-md-editor" in dependencies
    - package.json contains "vitest" in devDependencies
    - package.json contains "@radix-ui/react-alert-dialog" in dependencies
    - src/components/ui/alert-dialog.tsx exists
    - src/lib/utils.ts exists and exports function cn
    - vitest.config.ts exists in project root
    - vitest.config.ts contains environment: 'node'
    - vitest.config.ts contains include: ['tests/**/*.test.ts']
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tag-normalize and actions unit tests</name>
  <files>tests/tag-normalize.test.ts, tests/actions.test.ts</files>
  <read_first>
    - /Users/zacharylim/Documents/Claude/Projects/dev-journal/src/lib/db/schema.ts — table definitions; tests/actions.test.ts must replicate the SQL schema exactly (column names, types, constraints)
    - /Users/zacharylim/Documents/Claude/Projects/dev-journal/.planning/phases/03-write-loop/03-PATTERNS.md §tests/tag-normalize.test.ts and §tests/actions.test.ts — complete recommended test bodies
    - /Users/zacharylim/Documents/Claude/Projects/dev-journal/.planning/phases/03-write-loop/03-VALIDATION.md §Wave 0 Requirements — confirms required test coverage
  </read_first>
  <behavior>
    tests/tag-normalize.test.ts:
    - normalizeTag('  Foo  ') === 'foo' (trim + lowercase)
    - normalizeTag('') === '' (empty passthrough)
    - normalizeTag('  Café  ') === 'café' (unicode lowercase)
    - server-side pipeline: ['  Foo  ', 'bar', '  Foo  ', '', 'BAZ'] normalizes to ['foo', 'bar', 'baz']

    tests/actions.test.ts:
    - createEntry pattern: insert row, .returning().all() returns array of length 1 with id === 1
    - updateEntry pattern: insert then update; title + body reflect new values
    - deleteEntry with cascade: insert entry + tag + entry_tag; delete entry; both entry and entry_tag rows gone
    - setEntryTags upsert: insert existing tag with onConflictDoNothing; no duplicate rows
  </behavior>
  <action>
    Create tests/tag-normalize.test.ts with Vitest describe/it/expect blocks. The file must define a local normalizeTag(raw: string): string function (trim + toLowerCase) and the server-side pipeline logic (Set + map + filter(Boolean)) as inline logic under test. Do not import from src/ — keep these as pure logic tests.

    Create tests/actions.test.ts using better-sqlite3 with an in-memory Database(':memory:'). In beforeEach, create the tables with raw SQL matching the actual schema in src/lib/db/schema.ts (entries table: id INTEGER PRIMARY KEY AUTOINCREMENT, title TEXT NOT NULL DEFAULT '', body TEXT NOT NULL DEFAULT '', created_at INTEGER NOT NULL DEFAULT (unixepoch()), updated_at INTEGER NOT NULL DEFAULT (unixepoch()); tags table: id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE; entry_tags table: entry_id INTEGER NOT NULL REFERENCES entries(id) ON DELETE CASCADE, tag_id INTEGER NOT NULL REFERENCES tags(id) ON DELETE CASCADE, PRIMARY KEY(entry_id, tag_id)). Enable foreign key enforcement with `pragma foreign_keys = ON`.

    Import drizzle from 'drizzle-orm/better-sqlite3' and import schema types from '@/lib/db/schema' for the Drizzle query layer. Use ONLY the Drizzle query builder in test bodies — no raw SQL in assertions. Use .all() for selects (not .get() — deprecated per RESEARCH.md §State of the Art).

    After writing both test files, run: npx vitest run — all tests must pass before this task is complete. If any test fails, fix the test to match the actual schema/behavior, not the other way around.
  </action>
  <verify>
    <automated>cd /Users/zacharylim/Documents/Claude/Projects/dev-journal && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>npx vitest run exits 0; all describe blocks in both test files show green; no TypeScript errors in test files</done>
  <acceptance_criteria>
    - tests/tag-normalize.test.ts exists with at least 4 it() blocks covering trim, lowercase, empty, and server-side pipeline
    - tests/actions.test.ts exists with at least 4 describe blocks (createEntry, updateEntry, deleteEntry, setEntryTags)
    - tests/actions.test.ts uses Database(':memory:') in beforeEach with foreign_keys ON pragma
    - tests/actions.test.ts uses .all() not .get() for all Drizzle selects
    - npx vitest run exits with code 0
    - npx vitest run output shows no red/failing tests
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party packages installed via npx shadcn and npm install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm/npx package installs (shadcn, vitest, @uiw/react-md-editor) | mitigate | All packages verified via npm registry in RESEARCH.md Package Legitimacy Audit; human checkpoint (Task 1) reviews install output before proceeding |
| T-03-SC | Tampering | shadcn CLI bootstrap — creates components.json + installs 5 packages | mitigate | Blocking human-verify checkpoint confirms files created and no unexpected packages installed; [ASSUMED] packages reviewed in RESEARCH.md |
</threat_model>

<verification>
Run after both tasks complete:
1. npx vitest run — exits 0, all tests green
2. ls tests/tag-normalize.test.ts tests/actions.test.ts vitest.config.ts — all three files present
3. ls src/components/ui/alert-dialog.tsx src/lib/utils.ts — both generated by shadcn
4. grep -c "describe\|it(" tests/tag-normalize.test.ts — at least 4
5. grep -c "describe\|it(" tests/actions.test.ts — at least 4
</verification>

<success_criteria>
- All installed packages present in package.json
- src/components/ui/alert-dialog.tsx and src/lib/utils.ts (with cn() export) generated
- vitest.config.ts present with node environment
- npx vitest run passes with all tests green
- Both test files cover the behaviors declared in 03-VALIDATION.md §Wave 0 Requirements
</success_criteria>

<output>
Create .planning/phases/03-write-loop/03-01-SUMMARY.md when done
</output>
