---
phase: 04-search-filter
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/app/page.tsx
autonomous: false
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03

must_haves:
  truths:
    - "Home page renders SearchInput above the entry list on every visit (no filter active)"
    - "Typing in search box updates ?q= in URL and rerenders page with matching entries (SRCH-01)"
    - "Clicking a TagChip navigates to /?tag={name} and rerenders page with filtered entries (SRCH-02)"
    - "With both ?q= and ?tag= set, entry list shows only entries matching both (SRCH-03)"
    - "Result count label 'N entries' appears only when q or tag is set"
    - "ActiveFilterChip appears only when ?tag= is set; clicking × removes tag and preserves q"
    - "Empty-search state shows contextual copy when results.length === 0 and a filter is active"
    - "Unfiltered empty journal state is preserved (no regressions)"
    - "SearchInput is wrapped in <Suspense fallback={null}> — no hydration warning in next build"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page wired with searchParams, searchEntries, all new components"
      contains: "searchEntries"
    - path: "src/app/page.tsx"
      provides: "Suspense boundary around SearchInput"
      contains: "Suspense"
  key_links:
    - from: "src/app/page.tsx"
      to: "searchEntries(q, tag)"
      via: "await searchParams then call"
      pattern: "await searchEntries"
    - from: "src/app/page.tsx"
      to: "SearchInput"
      via: "Suspense boundary"
      pattern: "<Suspense"
    - from: "src/app/page.tsx"
      to: "ActiveFilterChip"
      via: "conditional render when tag is set"
      pattern: "{tag &&"
---

<objective>
Wire the home page to use searchEntries() with searchParams, compose all new components (SearchInput, ActiveFilterChip, result count, empty search state), and verify the complete end-to-end search and filter flow works in the browser.

Purpose: This plan delivers the user-visible feature. After this plan, all three v1 search requirements are live and all four ROADMAP.md Phase 4 success criteria are satisfied.

Output: Updated src/app/page.tsx; checkpoint human verification that search, filter, and combined flow all work correctly in the browser.
</objective>

<execution_context>
@/Users/zacharylim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zacharylim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/zacharylim/Documents/Claude/Projects/dev-journal/.planning/phases/04-search-filter/04-CONTEXT.md
@/Users/zacharylim/Documents/Claude/Projects/dev-journal/.planning/phases/04-search-filter/04-UI-SPEC.md
@/Users/zacharylim/Documents/Claude/Projects/dev-journal/.planning/phases/04-search-filter/04-RESEARCH.md

<interfaces>
From src/app/page.tsx (current — replace entirely, preserving empty-journal state copy):
```typescript
// Current signature — no searchParams prop, calls getEntries()
export default async function Home() {
  const entries = await getEntries();
  // ...
}
// Empty journal copy to preserve:
// "No entries yet." / "Your journal is empty. Entries you write will appear here."
// Entry list container: <div className="divide-y divide-zinc-200">
```

New imports needed (all from completed Plan 01 and Plan 02):
```typescript
import { searchEntries } from '@/lib/actions';        // Plan 01
import SearchInput from '@/components/search-input';   // Plan 02
import ActiveFilterChip from '@/components/active-filter-chip'; // Plan 02
import { Suspense } from 'react';
```

New page.tsx signature (Next.js 16 — searchParams is a Promise):
```typescript
export default async function Home({
  searchParams,
}: {
  searchParams: Promise<{ q?: string; tag?: string }>
}) {
  const { q, tag } = await searchParams;
  const entries = await searchEntries(q ?? null, tag ?? null);
  const isFiltered = Boolean(q || tag);
  // ...
}
```

UI-SPEC layout order (from 04-UI-SPEC.md lines 133–157):
1. <SearchInput defaultValue={q ?? ''} /> wrapped in <Suspense fallback={null}>
2. [if isFiltered] result count <p>
3. [if tag] <ActiveFilterChip tag={tag} currentQ={q} />
4. [if entries.length === 0 && isFiltered] EmptySearchState (inline JSX, not a component)
5. [if entries.length === 0 && !isFiltered] existing empty journal JSX
6. [if entries.length > 0] <div className="divide-y divide-zinc-200 mt-6"> entry list

Copywriting (from 04-UI-SPEC.md lines 161–179 — use EXACTLY):
- Result count: entries.length === 1 ? '1 entry' : `${entries.length} entries`
- No-results heading: "No entries found"
- No-results body keyword: `No entries match "${truncate(q, 40)}". Try a different keyword or clear the search.`
- No-results body tag: `No entries tagged "${tag}". Try a different tag or clear the filter.`
- No-results body combined: `No entries match "${truncate(q, 40)}" tagged "${tag}". Clear the search or filter to see all entries.`
- "Clear the search" links to `/?` (if no tag) or `/?tag=${tag}` (if tag present, preserves tag)
  Wait — UI-SPEC says "Clear the search" links to /?. "Clear the filter" links to /?q={q}.
  For combined no-results: "Clear the search or filter" — link to /

q truncation: if q && q.length > 40, display q.slice(0, 40) + '…'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire page.tsx with searchParams, searchEntries, and all components</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (full file — current content; will be replaced)
    - .planning/phases/04-search-filter/04-UI-SPEC.md (lines 130–179 — layout spec and copywriting contract)
    - .planning/phases/04-search-filter/04-PATTERNS.md (lines 120–191 — page.tsx updated signature, Suspense wrapper)
    - .planning/phases/04-search-filter/04-RESEARCH.md (lines 339–384 — Pattern 4: full Home component example; Pitfall 3: Suspense; Pitfall 5: await searchParams)
  </read_first>
  <action>
    Replace src/app/page.tsx entirely. Keep the existing file comment at the top.

    Imports: import { searchEntries } from '@/lib/actions' (replace getEntries import); import EntryCard from '@/components/entry-card' (keep); import SearchInput from '@/components/search-input'; import ActiveFilterChip from '@/components/active-filter-chip'; import { Suspense } from 'react'.

    Page signature: async function Home({ searchParams }: { searchParams: Promise<{ q?: string; tag?: string }> }). Await searchParams at the top of the function body. Call searchEntries(q ?? null, tag ?? null). Derive isFiltered = Boolean(q || tag).

    Helper inline in the file (not exported): function truncateQ(s: string | undefined): string — returns s!.slice(0, 40) + (s!.length > 40 ? '…' : '') — only called when s is truthy.

    JSX structure inside <main className="max-w-5xl mx-auto px-4 py-8">:

    1. Always render: <Suspense fallback={null}><SearchInput defaultValue={q ?? ''} /></Suspense>

    2. If isFiltered: <p className="mt-3 text-sm text-muted-foreground" aria-live="polite">{entries.length === 1 ? '1 entry' : `${entries.length} entries`}</p>

    3. If tag is set: <ActiveFilterChip tag={tag} currentQ={q} />

    4. If entries.length === 0 && isFiltered: inline EmptySearchState JSX —
       - Outer div: className="py-16 text-center"
       - Heading <p> className="text-zinc-500 text-sm font-medium": "No entries found"
       - Body <p> className="mt-1 text-zinc-400 text-xs": conditional copy from copywriting contract (q only / tag only / both)
       - For keyword no-results body: include an <a href={tag ? `/?tag=${encodeURIComponent(tag)}` : '/'} className="underline hover:text-zinc-600">clear the search</a> link inline in the text.
       - For tag no-results body: include an <a href={q ? `/?q=${encodeURIComponent(q)}` : '/'} className="underline hover:text-zinc-600">clear the filter</a> link inline in the text.
       - For combined no-results body: include an <a href="/" className="underline hover:text-zinc-600">clear the search or filter</a> link.

    5. If entries.length === 0 && !isFiltered: existing empty journal JSX (copy from current file exactly — "No entries yet." / "Your journal is empty. Entries you write will appear here.").

    6. If entries.length > 0: <div className="divide-y divide-zinc-200 mt-6">{entries.map(...)}</div> — note mt-6 added (was absent in current file).

    Do not add 'use client' — page.tsx remains a Server Component.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "page.tsx"</automated>
  </verify>
  <done>src/app/page.tsx contains searchEntries, Suspense, SearchInput, ActiveFilterChip, aria-live="polite" on result count; npx tsc --noEmit reports no errors for page.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify search and filter end-to-end in browser</name>
  <what-built>
    Plan 03 Task 1 wired page.tsx to use searchEntries(), SearchInput, ActiveFilterChip, result count, and empty states. Plans 01–02 built the data layer and client components. The full search feature is now assembled.
  </what-built>
  <how-to-verify>
    Run: npm run dev (if not already running). Visit http://localhost:3000.

    Check 1 — Search input visible:
    - Home page shows the search input at the top of the content area (below header, above entry list)
    - Input has a magnifying glass icon on the left
    - Placeholder text reads "Search entries..."

    Check 2 — Keyword search (SRCH-01):
    - Type a word that appears in one of your entries (e.g., a project name or technology you've written about)
    - After ~300ms, the URL changes to /?q={word} and the entry list filters to matching entries
    - Result count label "N entries" appears between the search input and the list
    - Clear the input — URL returns to / and all entries reappear (no crash)

    Check 3 — Tag filter (SRCH-02):
    - Click any TagChip on an entry card or entry detail page
    - URL changes to /?tag={tagname} and the list filters to entries with that tag
    - An "Active filter: {tagname} ×" chip appears between the result count and the entry list
    - Clicking × on the chip returns to / (all entries)

    Check 4 — Combined search + filter (SRCH-03):
    - With a tag filter active (/?tag=something), type in the search box
    - URL updates to /?q={word}&tag={something} — both params present
    - Entry list shows only entries that match BOTH the keyword and the tag
    - Clearing the search input (backspace to empty) returns to /?tag={something} — tag preserved

    Check 5 — Empty states:
    - Search for a word that matches nothing (e.g., "xyzzy123")
    - "No entries found" heading and contextual body copy appear with a "clear the search" link
    - Click "clear the search" — returns to all entries

    Check 6 — No regressions:
    - Navigate to an entry detail page — TagChips still display correctly
    - Navigate to the edit page — EditorTagChip (delete-only) still functions (is NOT affected by TagChip change)
    - npm run build completes without errors or "useSearchParams() should be wrapped in Suspense" warnings
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass. Describe any issues found if checks fail.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → page.tsx | q and tag arrive from the browser URL; page.tsx reads them as server-side props |
| page.tsx → JSX render | tag and q values are rendered into JSX text nodes (not innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | page.tsx renders q in no-results copy | accept | React renders {q} as an escaped text node; dangerouslySetInnerHTML is never used; XSS not possible via JSX string interpolation |
| T-04-09 | Tampering | page.tsx renders tag in ActiveFilterChip and no-results copy | accept | Same as T-04-08; all values are text nodes escaped by React |
| T-04-10 | Tampering | "clear the search/filter" links built from q/tag values | mitigate | Links built with encodeURIComponent(q) and encodeURIComponent(tag) in href attributes; prevents URL injection |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — package legitimacy gate not applicable |
</threat_model>

<verification>
npx tsc --noEmit — no errors
npx vitest run — full suite green
npm run build — completes without errors; no "useSearchParams should be wrapped in Suspense" warning
Browser: http://localhost:3000 — search input visible, debounce works, tag filter works, combined works, empty state works, × dismiss works
</verification>

<success_criteria>
- src/app/page.tsx uses searchParams: Promise<{ q?: string; tag?: string }> and awaits it
- searchEntries(q ?? null, tag ?? null) called instead of getEntries()
- SearchInput wrapped in <Suspense fallback={null}>
- Result count label rendered with aria-live="polite" only when isFiltered is true
- ActiveFilterChip rendered only when tag is set, with currentQ prop passed
- Three distinct no-results copy variants (keyword only, tag only, combined) with inline clear links
- Existing empty-journal state preserved verbatim
- Entry list div has mt-6 margin
- Human checkpoint: all 6 browser checks pass
- ROADMAP.md Phase 4 success criteria all satisfied:
  1. Typing in search shows results in real time (debounced) — SRCH-01
  2. Clicking tag chip filters entry list — SRCH-02
  3. Keyword and tag filter work simultaneously — SRCH-03
  4. Empty string / clearing search returns all entries without crash — empty guard
</success_criteria>

<output>
Create .planning/phases/04-search-filter/04-03-SUMMARY.md when done.
</output>
